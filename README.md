# Sacred Science Prototype

An interactive React prototype simulating a modern Victorian mansion for exploring philosophical and metaphysical concepts.

## How to Run Locally

### Prerequisites
1.  Install **Node.js** (v16 or higher) from [nodejs.org](https://nodejs.org/).
2.  Install a code editor like **VS Code**.

### Installation Steps

1.  **Download/Unzip** the project folder.
2.  Open your terminal (Command Prompt or Terminal) and navigate to the project folder.
    ```bash
    cd path/to/project-folder
    ```
3.  **Install Dependencies**:
    Run the following command to download the required libraries defined in `package.json`.
    ```bash
    npm install
    ```

### Running the App

1.  **Start the Development Server**:
    ```bash
    npm run dev
    ```
2.  The terminal will show a local URL (usually `http://localhost:5173/`).
3.  Open that URL in your browser to view the prototype.

### Building for Production

To create a static build for deployment:
```bash
npm run build
```
The output will be in the `dist` folder.
# Sassy
